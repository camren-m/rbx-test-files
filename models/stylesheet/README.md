# Roblox Studio Version: 0.715.0.7151115

Contains a single `StyleSheet` with `StyleRule`s.

* `stylesheet` (StyleSheet)
	* `.RootSelector` (StyleRule)
		* Property `Selector`: `.RootSelector`
		* Property `PropertiesSerialize`:
            * BackgroundTransparency: `1.0`
            * Size: `UDim2.new(1.0, 0, 1.0, 0)`
        * `> UIListLayout` (StyleRule)
            * Property `Selector`: `> UIListLayout`
            * Property `PropertiesSerialize`:
                * FillDirection: `1` (Vertical)
